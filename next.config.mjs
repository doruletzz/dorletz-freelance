/** @type {import('next').NextConfig} */
const nextConfig = {
  // i18n: {
  //   locales: ["en", "ro"],
  //   defaultLocale: "en",
  //   localeDetection: true,
  // },
};

export default nextConfig;
